🧪 SUBJECT 4 — PATH Resolver
mini_pathfind

Goal: Search $PATH for an executable.

Requirements:

Write a function that:

takes a command name (“ls”)

splits $PATH

checks access() for each /path/ls

Print the full executable path.

Expected:

./mini_pathfind ls
Found: /bin/ls
