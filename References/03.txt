🧪 SUBJECT 3 — execve() Runner
mini_exec

Goal: Execute a system command only using execve.

Requirements:

Hardcode the command /bin/ls

Build an argv array

Call execve()

Expected:

mini_exec$ ./mini_exec
<ls output>