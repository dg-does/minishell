🧪 SUBJECT 7 — dup2() Redirections
mini_redirect

Goals:
Practice redirections exactly like minishell requires.

Exercises:

Redirect stdout to file:

ls > out.txt


Redirect stdin from file:

wc -l < in.txt


Combine dup2() + execve()

💥 This is exactly minishell's redirection layer.