⭐ BONUS SUBJECT — The first real pipeline executor
mini_executor

Goal: Implement a miniature minishell executor without parser.

Input format:

./mini_executor "ls" "|" "wc" "-l"


Requirements:

Build array of commands manually

Fork children for each

Connect pipes

Execve each command

Close pipe ends properly

This is a realistic early prototype of your minishell executor.